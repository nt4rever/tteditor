import React from "react";
import "./Loading.css";

const Loading = () => {
    return (
        <div className="bg-loader">
            <span class="loader"></span>
        </div>
    );
};

export default Loading;
